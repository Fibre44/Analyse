{% extends 'base.html.twig'%}

{%block body %}

    <h1>Créer une rubrique</h1>

    {{form_start(form)}}
    {{form_widget(form)}}
    <button type="submit" class="btn btn-succes">Créer une rubrique</button>

    {{form_end(form)}}




{%endblock%}
