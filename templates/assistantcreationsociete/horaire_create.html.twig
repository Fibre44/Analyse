{% extends 'base_assistant.html.twig' %}

{%block body %}

    <h1>Créer un horaire</h1>

     {{form_start(formHoraire)}}
   
    {{form_widget(formHoraire)}}

    <button type="submit" class="btn btn-primary">Créer une journée</button>

    {{form_end(formHoraire)}}


<a class="btn btn-primary" href="{{ path('assistantcreationsociete_calendrier_create',{'idprojet':projet.id,'idsociete':societe.id})}}">Créer un nouveau calendrier</a>
</br>
<a class="btn btn-primary" href="{{ path('assistantcreationsociete_etablissement_create',{'idprojet':projet.id,'idsociete':societe.id})}}">Créer un etablissement</a>



{%endblock%}
