{% extends 'base_assistant.html.twig' %}
{%block body %}

    <h1>Créer une société</h1>

    {{form_start(form)}}
    {{form_widget(form)}}
    <button type="submit" class="btn btn-succes">Créer une société</button>

    {{form_end(form)}}




{%endblock%}

