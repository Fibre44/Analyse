{% extends 'base.html.twig'%}

{%block body %}

    <h1>Créer un établissement</h1>

    {{form_start(form)}}
    {{form_widget(form)}}
    <button type="submit" class="btn btn-succes">Créer un établissement</button>

    {{form_end(form)}}




{%endblock%}
